{
  "name": "x-reply-bot",
  "version": "0.2.0",
  "description": "X.com auto-reply bot. Telegram UI + scheduling, Chrome extension does the actual HTTP via local WebSocket bridge.",
  "type": "module",
  "main": "src/index.js",
  "scripts": {
    "start": "node src/index.js",
    "dev": "node --watch src/index.js"
  },
  "engines": {
    "node": ">=20"
  },
  "dependencies": {
    "better-sqlite3": "^11.5.0",
    "dotenv": "^16.4.5",
    "node-telegram-bot-api": "^0.66.0",
    "ws": "^8.18.0"
  }
}
